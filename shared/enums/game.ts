export enum GameDifficulty {
  Easy = 'easy',
  Medium = 'medium',
  Hard = 'hard',
}

export enum GameType {
  Boolean = 'boolean',
  Multiple = 'multiple',
}

// We need booleans as strings in order to compare it to the API response
export enum BooleanOptionType {
  True = 'True',
  False = 'False',
}
